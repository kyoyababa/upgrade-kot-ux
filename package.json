{
  "name": "upgrade-kot-ux",
  "version": "1.0.0",
  "description": "Upgrade KOT userability experience; 1-click page-moving, 1-click 'dakoku' request.",
  "scripts": {
    "start": "npm run clean && npm run prepare && (npm run build:image & npm run build:json) && npm run build:js",
    "clean": "rm -rf dest",
    "build:js": "parcel watch workspace/assets/scripts/upgradeKotUx__background.js -d dest/assets/scripts/ & parcel watch workspace/assets/scripts/upgradeKotUx__content.ts -d dest/assets/scripts/",
    "build:image": "cp -r workspace/assets/images dest/assets",
    "build:json": "cp workspace/manifest.json dest",
    "prepare": "mkdir dest"
  },
  "author": "kyoyababa",
  "license": "MIT",
  "standard": {
    "globals": [
      "chrome"
    ]
  },
  "devDependencies": {
    "@types/chrome": "0.0.93",
    "@types/jquery": "^3.3.32",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.6.1",
    "jquery": "^3.2.1",
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.7.5"
  }
}
